import React from "react";

const Login = () => {
  return (
    <div
      style={{
        display: "grid",
        gridTemplateColumns: `repeat(2 1fr)`,
      }}
    >
      Hola mundo!
    </div>
  );
};

export default Login;
